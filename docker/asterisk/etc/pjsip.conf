;==============================================================================
; PJSIP - Callbot
;
; DEV : endpoints "softphone-1" y "softphone-2" para probar con Linphone/Zoiper
; PROD: descomentar el bloque de troncal y setear TRUNK_* en el .env
;==============================================================================

[transport-udp]
type = transport
protocol = udp
bind = 0.0.0.0:5060
; IMPORTANTE en producción: descomentar y poner la IP pública del servidor
; y la red local de Docker, si no el audio se corta por NAT.
; external_media_address = 200.x.x.x
; external_signaling_address = 200.x.x.x
; local_net = 172.16.0.0/12
; local_net = 192.168.0.0/16

;------------------------------------------------------------------------------
; Plantillas reutilizables
;------------------------------------------------------------------------------
[endpoint-softphone](!)
type = endpoint
context = callbot-inbound
disallow = all
allow = ulaw
allow = alaw
direct_media = no
force_rport = yes
rewrite_contact = yes
rtp_symmetric = yes

[auth-userpass](!)
type = auth
auth_type = userpass

[aor-single](!)
type = aor
max_contacts = 1
remove_existing = yes
qualify_frequency = 30

;------------------------------------------------------------------------------
; Softphone 1 - usalo para recibir las llamadas del bot en desarrollo
;   Usuario: softphone-1   Password: ${SOFTPHONE_PASSWORD}   Servidor: IP_DEL_HOST
;------------------------------------------------------------------------------
[softphone-1](endpoint-softphone)
auth = softphone-1
aors = softphone-1
callerid = Softphone 1 <1001>

[softphone-1](auth-userpass)
password = ${SOFTPHONE_PASSWORD}
username = softphone-1

[softphone-1](aor-single)

;------------------------------------------------------------------------------
; Softphone 2 - segundo cliente de prueba
;------------------------------------------------------------------------------
[softphone-2](endpoint-softphone)
auth = softphone-2
aors = softphone-2
callerid = Softphone 2 <1002>

[softphone-2](auth-userpass)
password = ${SOFTPHONE_PASSWORD}
username = softphone-2

[softphone-2](aor-single)

;------------------------------------------------------------------------------
; TRONCAL SIP (producción) - descomentar y completar TRUNK_* en el .env
;------------------------------------------------------------------------------
; [trunk-proveedor]
; type = endpoint
; context = callbot-inbound
; disallow = all
; allow = ulaw
; allow = alaw
; outbound_auth = trunk-auth
; aors = trunk-aor
; from_user = ${TRUNK_USER}
; from_domain = ${TRUNK_HOST}
; direct_media = no
; rtp_symmetric = yes
; force_rport = yes
; rewrite_contact = yes
;
; [trunk-auth]
; type = auth
; auth_type = userpass
; username = ${TRUNK_USER}
; password = ${TRUNK_PASSWORD}
;
; [trunk-aor]
; type = aor
; contact = sip:${TRUNK_HOST}
; qualify_frequency = 60
;
; [trunk-registration]
; type = registration
; transport = transport-udp
; outbound_auth = trunk-auth
; server_uri = sip:${TRUNK_HOST}
; client_uri = sip:${TRUNK_USER}@${TRUNK_HOST}
; retry_interval = 60
;
; [trunk-identify]
; type = identify
; endpoint = trunk-proveedor
; match = ${TRUNK_HOST}
